package com.example.weatherapp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class NotificationConfig {
    // Configuration for push notification services (e.g., FCM) would go here.
    // This might involve setting up beans for the service's SDK.
}